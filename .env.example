# Tinder credentials (use Facebook or phone login)
TINDER_EMAIL=your@email.com
TINDER_PASSWORD=yourpassword

# Claude AI (uses your claude.ai subscription via browser — no API key needed!)

# Bot configuration
SWIPE_LIMIT=100
SWIPE_DELAY_MIN=1500
SWIPE_DELAY_MAX=4000
LIKE_RATIO=0.7
SESSION_FILE=sessions/tinder_session.json

# Messaging config
YOUR_NAME=Lukáš
YOUR_BIO=Write a short bio so Claude knows how to represent you
OPENER_STYLE=witty
