# Claude AI (uses your claude.ai subscription via browser — no API key needed!)

# Bot configuration
SWIPE_LIMIT=40
SWIPE_DELAY_MIN=3000
SWIPE_DELAY_MAX=8000
LIKE_RATIO=0.35

# Browser fingerprint (match your actual location to avoid bot detection)
BROWSER_LOCALE=cs-CZ
BROWSER_TIMEZONE=Europe/Prague

# Messaging config
YOUR_NAME=Lukáš
YOUR_AGE=21
YOUR_TINDER_NAME=Matej
YOUR_CITY=Prague
YOUR_BIO=Write a short bio so Claude knows how to represent you
OPENER_STYLE=witty

# Debug
DEBUG_SCREENSHOTS=false
